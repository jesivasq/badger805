class GuessView {
  constructor(element, parent) {
    this.parent = parent;
    this.element = element;
  }

  render() {
    // for character in this.model.guess
    // Update the element with that value.
    // use data from this.model to update the dom
  }
}
